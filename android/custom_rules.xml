<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:ivy="antlib:org.apache.ivy.ant">

  <property name="ivy.install.version" value="2.4.0" />
  <property name="jar.libs.dir" value="libs" />

  <condition property="ivy.home" value="${env.IVY_HOME}">
    <isset property="env.IVY_HOME" />
  </condition>
  <property name="ivy.home" value="${user.home}/.ant" />
  <property name="ivy.jar.dir" value="${ivy.home}/lib" />
  <property name="ivy.jar.file" value="${ivy.jar.dir}/ivy.jar" />

  <target name="-download-ivy">
    <mkdir dir="${ivy.jar.dir}" />
    <get src="http://repo2.maven.org/maven2/org/apache/ivy/ivy/${ivy.install.version}/ivy-${ivy.install.version}.jar" dest="${ivy.jar.file}" usetimestamp="true" />
  </target>

  <target name="-init-ivy" depends="-download-ivy">
    <path id="ivy.lib.path">
      <fileset dir="${ivy.jar.dir}" includes="*.jar" />
    </path>
    <taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpathref="ivy.lib.path" />
  </target>

  <target name="copy-local-deps">
    <copy file="../third_party/basic-http-client/libs/basic-http-client-android-0.88.jar" tofile="libs/basic-http-client-android-0.88.jar" />
    <copy file="../third_party/glide/library/libs/glide-3.2.0a.jar" tofile="libs/glide-3.2.0a.jar" />
  </target>

  <target name="retrieve" depends="-init-ivy,copy-local-deps" description="--> retrieve dependencies with ivy">
    <ivy:retrieve pattern="${jar.libs.dir}/[artifact](-[revision])(-[classifier]).[ext]" />
  </target>
</project>
